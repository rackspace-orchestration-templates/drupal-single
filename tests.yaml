test-cases:
- name: Default Build Test # Deploy using all default options
  create:
    timeout: 30 # Deployment should complete in under 30 minutes
  resource_tests: # Tests to run on the resources themselves
    ssh_private_key: { get_output: private_key } # Fetch from output-list of stack
    ssh_key_file: tmp/private_key # File to write with ssh_private_key
    tests:
    - drupal_server:
        fabric:
          # Fabric environment settings to use while running envassert script
          # http://docs.fabfile.org/en/latest/usage/env.html
          env:
            user: root
            key_filename: tmp/private_key
            hosts: { get_output: server_ip } # Fetch from output-list of stack
            tasks:
              - check
            abort_on_prompts: True
            connection_attempts: 3
            disable_known_hosts: True
            use_ssh_config: True
            fabfile: test/fabric/drupal.py # Path to envassert test

- name: Standard Instance, Ubuntu 12.04
  create:
    parameters:
      flavor: 2GB Standard Instance
      image: Ubuntu 12.04 LTS (Precise Pangolin)
    timeout: 30
  resource_tests:
    ssh_private_key: { get_output: private_key }
    ssh_key_file: tmp/private_key
    tests:
    - drupal_server:
        fabric:
          env:
            user: root
            key_filename: tmp/private_key
            hosts: { get_output: server_ip }
            tasks:
              - check
            abort_on_prompts: True
            connection_attempts: 3
            disable_known_hosts: True
            use_ssh_config: True
            fabfile: test/fabric/drupal.py

- name: Standard Instance, Ubuntu 14.04
  create:
    parameters:
      flavor: 2GB Standard Instance
      image: Ubuntu 14.04 LTS (Trusty Tahr) (PVHVM) 
    timeout: 30
  resource_tests:
    ssh_private_key: { get_output: private_key }
    ssh_key_file: tmp/private_key
    tests:
    - drupal_server:
        fabric:
          env:
            user: root
            key_filename: tmp/private_key
            hosts: { get_output: server_ip }
            tasks:
              - check
            abort_on_prompts: True
            connection_attempts: 3
            disable_known_hosts: True
            use_ssh_config: True
            fabfile: test/fabric/drupal.py

- name: Standard Instance, Debian 7
  create:
    parameters:
      flavor: 2GB Standard Instance
      image: Debian 7 (Wheezy) (PVHVM)
    timeout: 30
  resource_tests:
    ssh_private_key: { get_output: private_key }
    ssh_key_file: tmp/private_key
    tests:
    - drupal_server:
        fabric:
          env:
            user: root
            key_filename: tmp/private_key
            hosts: { get_output: server_ip }
            tasks:
              - check
            abort_on_prompts: True
            connection_attempts: 3
            disable_known_hosts: True
            use_ssh_config: True
            fabfile: test/fabric/drupal.py

- name: Standard Instance, CentOS 6
  create:
    parameters:
      flavor: 2GB Standard Instance
      image: CentOS 6.5 (PVHVM)
    timeout: 30
  resource_tests:
    ssh_private_key: { get_output: private_key }
    ssh_key_file: tmp/private_key
    tests:
    - drupal_server:
        fabric:
          env:
            user: root
            key_filename: tmp/private_key
            hosts: { get_output: server_ip }
            tasks:
              - check
            abort_on_prompts: True
            connection_attempts: 3
            disable_known_hosts: True
            use_ssh_config: True
            fabfile: test/fabric/drupal.py

- name: Standard Instance, Redhat 6
  create:
    parameters:
      flavor: 2GB Standard Instance
      image: Red Hat Enterprise Linux 6.5 (PVHVM)
    timeout: 30
  resource_tests:
    ssh_private_key: { get_output: private_key }
    ssh_key_file: tmp/private_key
    tests:
    - drupal_server:
        fabric:
          env:
            user: root
            key_filename: tmp/private_key
            hosts: { get_output: server_ip }
            tasks:
              - check
            abort_on_prompts: True
            connection_attempts: 3
            disable_known_hosts: True
            use_ssh_config: True
            fabfile: test/fabric/drupal.py
